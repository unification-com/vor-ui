const {
  getOracles,
  getOracleRequests,
  getRequestDetail,
  getOracleFeeHistory,
  getOracleSummary,
} = require("./api")

module.exports = {
  getOracles,
  getOracleRequests,
  getRequestDetail,
  getOracleFeeHistory,
  getOracleSummary,
}
